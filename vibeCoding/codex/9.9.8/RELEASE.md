# Athena Codex 9.9.8

Status: implementation in progress. Baseline: 9.9.6-hotfix2.

Same semantic contract as CC 9.9.8. Codex CLI 0.150.0 measured. 0.146+ `/export` is not the unique native_output_ref path until V2 pin is proven.

`permissions.defaultMode` on CC stays `default`; Claude Code 2.1.200+ also accepts `manual` as a `default` alias (documented for dual-end migrate).
