# 控制指令

---

## /vibe-status - 查看状态

### 作用
读取并显示当前工作流状态。

### 输出
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 工作流状态

模式: [workflow/idle]
路径: [A/B/C]
阶段: [R1/I/P/E/R2]
进度: [X/Y] (XX%)

## 当前 TODO
- [x] T-001 ✅
- [ ] T-002 ⏳ ← 当前
- [ ] T-003

## 选项
- 继续: 输入任意内容
- 暂停: /vibe-pause
- 中止: /vibe-abort
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## /vibe-pause - 暂停工作流

### 作用
保存当前状态，暂停工作流。

### 执行步骤
```javascript
// 1. 更新 session.lock
session.lock.mode = "paused"
session.lock.paused_at = new Date()
session.lock.resume_point = currentTask

// 2. 保存状态
saveState()

// 3. 输出
```

### 输出
```
⏸️ 工作流已暂停

保存点: T-003 (前端组件开发)
进度: 2/5 完成

恢复方式:
- 输入 /vibe-resume 继续
- 或直接说 "继续"

状态已保存，可以安全关闭会话。
```

---

## /vibe-resume - 恢复工作流

### 作用
从暂停点恢复工作流。

### 执行步骤
```javascript
// 1. 读取 session.lock
const lock = readSessionLock()

// 2. 检查是否有暂停的工作流
if (!lock || lock.mode !== "paused") {
  return "没有暂停的工作流"
}

// 3. 恢复状态
lock.mode = "workflow"
saveSessionLock(lock)

// 4. 从 resume_point 继续
continueFrom(lock.resume_point)
```

### 输出
```
▶️ 工作流已恢复

恢复点: T-003 (前端组件开发)
当前进度: 2/5 完成

继续执行 T-003...
```

---

## /vibe-abort - 中止工作流

### 作用
放弃当前工作流（需确认）。

### 执行步骤
```javascript
// 1. 确认
await cunzhi.confirm({
  message: "确定要中止当前工作流吗？\n进度将丢失。",
  options: ["确认中止", "取消"]
})

// 2. 如果确认
if (confirmed) {
  // 删除 session.lock
  rm(".ai_state/session.lock")
  
  // 可选：保留或清理状态文件
}
```

### 输出
```
⚠️ 确定要中止当前工作流吗？

当前进度: 2/5 (40%)
已完成任务将保留在 kanban 中。

选择: 确认中止 / 取消
```
