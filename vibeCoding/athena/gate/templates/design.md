---
sprint: "{{sprint}}"
path: "{{path}}"
created: "{{date}}"
req: "{{req}}"
roadmap: "{{roadmap}}"
item: "{{item}}"
base_commit: "{{base_commit}}"
review_ignore: []
---

# Design — {{title}}

## 背景

<!-- 为什么做；链接 req / roadmap。 -->

## 方案

<!-- 最小可行方案；取舍与反对理由。 -->

## 验收标准

<!-- 每行一条，行首 `- ACn: <可观测结果>`；验证方式写成 `验证: athena run --covers ACn -- <命令>`。
- AC1: TODO（占位，H1 不认；改成可观测结果）
-->

## 允许写集

## 不做
