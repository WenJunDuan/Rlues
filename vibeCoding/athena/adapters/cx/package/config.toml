# VibeCoding Athena · Codex Configuration
# 位置: ~/.codex/config.toml
# 仅首装生效：已有 ~/.codex/config.toml 时安装器保留用户文件，只改写 VIBECODING_VERSION。
# 原生 schema: https://learn.chatgpt.com/docs/config-file/config-reference

project_root_markers = [".git", ".hg", ".sl"]
project_doc_max_bytes = 65536

model_provider = "openai"
model = "gpt-6-sol"
model_reasoning_effort = "high"
plan_mode_reasoning_effort = "xhigh"
web_search = "live"
suppress_unstable_features_warning = true

# 官方默认：可写工作区 + 需要时询问。完全放开（never + danger-full-access）官方标注 not recommended，
# 需要时在个人 ~/.codex/config.toml 或单次 `codex --yolo` 中自行开启。
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
# 官方默认关；Athena 需要装依赖 / 拉文档，开启。仅在 workspace-write 下生效。
network_access = true

[features]
memories = true
prevent_idle_sleep = true

[memories]
generate_memories = true
use_memories = true
disable_on_external_context = true

[features.multi_agent_v2]
tool_namespace = "agents"
enabled = true
max_concurrent_threads_per_session = 9
hide_spawn_agent_metadata = false

[shell_environment_policy]
inherit = "core"

[shell_environment_policy.set]
VIBECODING_VERSION = "{{athena:ATHENA_VERSION}}"

# Codex 桌面端内置插件（ID 来自 Codex 应用，官方文档未列出；CLI 无此插件时忽略）
[plugins."browser@openai-bundled"]
enabled = true

[plugins."computer-use@openai-bundled"]
enabled = true

[plugins."documents@openai-primary-runtime"]
enabled = true

[plugins."pdf@openai-primary-runtime"]
enabled = true

[plugins."spreadsheets@openai-primary-runtime"]
enabled = true

[plugins."presentations@openai-primary-runtime"]
enabled = true
