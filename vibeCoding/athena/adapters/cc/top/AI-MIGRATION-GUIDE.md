# Athena · Claude Code · 安装与迁移

本文件只做指引。Athena 文档单源在 `vibeCoding/athena/`，安装后位于 `~/.athena/<ver>/`：

| 文档 | 内容 |
|---|---|
| `INSTALL.md` | 给 AI 的安装 / 升级 / 回滚步骤 |
| `MIGRATION.md` | 项目 `.ai_state` v1 → v2 迁移 |
| `RELEASE.md` | 版本发布声明 |
| `CHANGELOG.md` | 完整变更记录 |

9.9.x 及更早的版本说明随旧包归档在 `vibeCoding/old/`。
