# Claude Code 标准配置 v7.0

> AI 编程协作专家系统 - RIPER-10 + 寸止协议

## 核心特性

- **寸止协议**: 唯一用户交互方式，禁止直接询问
- **RIPER-10**: R1 感知 →I 设计 →P 锁定 →E 执行 →R2 闭环
- **P.A.C.E.路由**: 智能分流 Path A/B/C
- **promptx 角色**: PM/PDM/AR/LD/QE/SA/DW
- **Skills 系统**: codex/memory/sou/thinking 等

## 目录结构

```
.claude/
├── CLAUDE.md              # 🔑 主入口（铁律+启动协议）
├── commands/              # 命令定义
│   ├── dev.md            # 完整开发(Path B/C)
│   ├── quick.md          # 快速开发(Path A)
│   ├── architect.md      # 架构设计
│   ├── review.md         # 代码审查
│   └── debug.md          # 系统调试
├── agents/                # 角色定义(via promptx)
│   ├── pm.md             # 项目经理
│   ├── pdm.md            # 产品经理
│   ├── ar.md             # 架构师
│   ├── ld.md             # 开发工程师
│   ├── qe.md             # 测试工程师
│   ├── sa.md             # 安全审计
│   └── dw.md             # 技术文档
├── skills/                # 技能定义
│   ├── codex/            # 代码执行
│   ├── memory/           # 记忆管理
│   ├── sou/              # 语义搜索
│   ├── thinking/         # 深度推理
│   ├── task-manager/     # 任务管理
│   ├── deepwiki/         # 技术文档
│   └── devtools/         # 浏览器测试
├── workflows/
│   ├── pace.md           # P.A.C.E.路由
│   ├── riper.md          # RIPER-10流程
│   └── cunji.md          # 寸止协议
└── references/
    ├── mcp-tools.md      # MCP工具参考
    └── code-standards.md # 代码规范
```

## 部署

```bash
# 复制到项目
cp -r .claude /your/project/

# 或全局配置
cp -r .claude ~/
```

## MCP 工具依赖

**必需**:

- 寸止 (用户交互)
- memory (记忆管理)
- codex (代码执行)

**推荐**:

- augment-context-engine (sou 搜索)
- sequential-thinking (深度推理)
- shrimp-task-manager (任务管理)
- promptx (角色切换)

**可选**:

- mcp-deepwiki, chrome-devtools, fetch, everything, context7

## 铁律

1. **禁止直接询问**: 只能通过`寸止`与用户交互
2. **默认静默执行**: 不创建文档/不测试/不编译
3. **未批准禁止结束**: 必须通过`寸止`获得确认
4. **工具优先于输出**: 能用工具解决的优先调用工具

## P.A.C.E.路由

| 路径   | 条件          | 流程        |
| :----- | :------------ | :---------- |
| Path A | 单文件/<30 行 | R1→E→R2     |
| Path B | 2-10 文件     | R1→I→P→E→R2 |
| Path C | >10 文件      | 分阶段迭代  |

## 扩展 Skills

可在 skills 目录添加 Python 脚本：

```
.claude/skills/
├── codex/SKILL.md
├── data-analysis/
│   ├── SKILL.md
│   └── analyze.py
└── report-gen/
    └── generate.py
```

---

**版本**: v5.1 | **协议**: RIPER-10 + 寸止 | **路由**: P.A.C.E.
