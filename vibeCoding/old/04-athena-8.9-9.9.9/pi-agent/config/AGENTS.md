# Athena · Pi

PACE 与门禁由 **athena-pace** 插件提供（`pi install` 本仓库 `vibeCoding/pi/plugin`）。
项目状态：`.ai_state/_index.md`。没有就 `/athena-init`。
铁律由插件每轮注入。此处只放个人偏好，不重复流程。

- 模型与第三方包见 `settings.json`
- 项目规范见 `rules/`（若已软链）
