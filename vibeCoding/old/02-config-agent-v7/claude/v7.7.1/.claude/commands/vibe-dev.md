# vibe-dev

## 类型
🔶 **纯自定义指令** - 需求研发主入口

## 用途
智能路由的需求研发入口，根据意图自动选择流程。

## 语法
```bash
vibe-dev <任务描述>           # 新建任务
vibe-dev <任务ID>             # 继续任务
vibe-dev <任务ID> --design    # 指定设计阶段
vibe-dev <任务ID> --develop   # 指定开发阶段
vibe-dev --path=A|B|C <描述>  # 指定路径
```

## 设计原则
`vibe-dev` 是智能入口，内部会**调用增强官方指令**：

```yaml
内部调用:
  - vibe-plan (→ /plan)      # 任务规划
  - vibe-todos (→ /todos)    # 任务列表
  - vibe-review (→ /review)  # 代码审查

这确保:
  - 官方功能正常工作
  - 官方统计正确记录
  - VibeCoding 增强叠加
```

## 执行流程

```
vibe-dev "实现用户认证"
    │
    ▼
┌─────────────────────────────┐
│ 1. Phase Router             │ ← phase-router skill
│    - 意图识别               │
│    - 复杂度评估             │
│    - 路径选择               │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ 2. 路由到 Agent             │
│    - requirement-mgr        │
│    - design-mgr             │
│    - impl-executor          │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ 3. 九步工作流               │
│    - 调用 vibe-plan (→/plan)│
│    - 调用 vibe-review (→/review)│
│    - 其他增强指令           │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ 4. 知识库 + 经验库          │ ← 全程检索
└─────────────────────────────┘
```

## 路由规则

### 基于意图
```yaml
新建需求:
  关键词: 新建, 创建, 添加, 实现
  路由: requirement-mgr → 需求创建

设计意图:
  关键词: 设计, 架构, 方案
  路由: design-mgr → 方案设计

开发意图:
  关键词: 开发, 实现, 编码
  路由: impl-executor → 开发实施

继续任务:
  输入: 任务ID
  路由: 查询状态 → 继续当前阶段
```

### 基于复杂度
```yaml
Path A: 单文件 & <30行
  → 简化九步 (1→6→7→9)

Path B: 2-10 文件
  → 标准九步 (1→2→3→4→6→7→9)

Path C: >10 文件或跨模块
  → 完整九步
```

## 内部调用链

### 需求阶段
```
vibe-dev "新功能"
    ↓
requirement-mgr
    ↓
knowledge-base skill (检索项目背景)
    ↓
experience skill (检索类似需求)
    ↓
riper/research skill (需求分析)
    ↓
[REQ_READY] 寸止
```

### 计划阶段
```
vibe-dev REQ-001 --develop
    ↓
impl-executor
    ↓
vibe-plan (→ 调用官方 /plan)  ← 确保官方功能
    ↓
knowledge-base + experience
    ↓
riper/plan skill
    ↓
[PLAN_READY] 寸止
```

### 审查阶段
```
开发完成
    ↓
vibe-review (→ 调用官方 /review)  ← 确保官方功能
    ↓
knowledge-base (审查规范)
    ↓
experience (常见问题)
    ↓
riper/review + code-quality
    ↓
经验沉淀
```

## 使用示例

### 新建需求
```bash
vibe-dev "添加用户搜索功能"

# 流程:
# 1. 识别: 新建需求
# 2. 路由: requirement-mgr
# 3. 检索: 知识库 + 经验库
# 4. 创建: REQ-xxx
# 5. [REQ_READY] 等待确认
```

### 继续任务
```bash
vibe-dev REQ-001

# 流程:
# 1. 查询: REQ-001 状态 (需求已确认)
# 2. 推断: 下一步是方案设计
# 3. 路由: design-mgr
# 4. 继续设计流程
```

### 指定路径
```bash
vibe-dev --path=A "修复登录按钮"

# 流程:
# 1. 强制 Path A
# 2. 简化九步 (1→6→7→9)
# 3. 快速完成
```

## 状态管理

### 创建/更新的文件
```yaml
需求阶段:
  - .ai_state/requirements/REQ-xxx.md

设计阶段:
  - .ai_state/designs/DESIGN-xxx.md

计划阶段:
  - .ai_state/meta/TODO.md
  - .ai_state/meta/kanban.md

全程:
  - .ai_state/meta/session.lock
  - .ai_state/meta/errors.md (如有)
```

## 与官方指令的关系
```yaml
vibe-dev 不替代官方指令，而是:
  1. 作为智能入口
  2. 内部调用增强官方指令
  3. 确保官方功能和统计正常

用户也可以直接使用:
  - vibe-plan → /plan
  - vibe-review → /review
  - 官方原生指令
```
