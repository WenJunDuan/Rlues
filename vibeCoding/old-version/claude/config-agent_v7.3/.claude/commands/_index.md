---
description: VibeCoding自定义指令定义，使用vibe-前缀避免与官方指令冲突
---

# VibeCoding Commands (vibe-前缀)

## 设计原则

1. **前缀隔离**: 所有自定义指令使用 `vibe-` 前缀，避免与官方Claude指令冲突
2. **分离架构**: 指令只负责调度，具体执行由角色(Agent)和技能(Skill)完成
3. **参数化**: 支持 `--skill=xxx` 指定执行技能

---

## 🚀 核心指令

| 指令 | 简写 | 描述 |
|:---|:---|:---|
| `/vibe-plan` | `/vp` | 深度规划模式 |
| `/vibe-design` | `/vd` | 架构设计模式 |
| `/vibe-code` | `/vc` | 编码执行模式 |
| `/vibe-review` | `/vr` | 代码审查模式 |
| `/vibe-debug` | `/vdb` | 问题调试模式 |

## 🔧 系统指令

| 指令 | 描述 |
|:---|:---|
| `/vibe-init` | 初始化项目，创建 `project_document/.ai_state/` |
| `/vibe-state` | 查看/同步当前状态 |
| `/vibe-clean` | 清除上下文，重新加载 |
| `/vibe-help` | 显示帮助信息 |

---

## 🛡️ 参数说明

### --skill 参数
指定执行技能（默认根据任务自动选择）：

```bash
/vibe-code --skill=codex "实现登录功能"
/vibe-code --skill=gemini "优化性能"  # 未来扩展
```

### --strict 参数
启用严格模式：

```bash
/vibe-review --strict  # 攻击性代码审查
```

### --tdd 参数
启用TDD模式：

```bash
/vibe-code --tdd "实现用户注册"
```

### --path 参数
强制指定P.A.C.E.路径：

```bash
/vibe-code --path=C "重构认证系统"  # 强制使用Path C逐步思考
```

---

## 📋 指令详情

### /vibe-plan (深度规划)

**加载**: `pdm` + `pm` + `meeting/` + `feature-dev`

**流程**:
```
需求澄清 → WBS分解 → 风险评估 → [PLAN_READY]
```

**输出**: 写入 `project_document/.ai_state/active_context.md`

---

### /vibe-design (架构设计)

**加载**: `ar` + `ui` + `thinking/` + `frontend-design`

**流程**:
```
问题分析 → 数据结构设计 → 方案对比 → [DESIGN_FREEZE]
```

**特点**: Data First，Linus审查

---

### /vibe-code (编码执行)

**加载**: `ld` + 指定skill + `commit-commands`

**流程**:
```
读取任务 → 执行 → 验证 → [PRE_COMMIT](大规模时)
```

**参数**:
- `--skill=codex` - 使用Codex执行
- `--skill=gemini` - 使用Gemini执行（未来）
- `--tdd` - TDD模式

---

### /vibe-review (代码审查)

**加载**: `qe` + `verification/` + `code-review` + `pr-review-toolkit` + `security-guidance`

**流程**:
```
变更分析 → 逻辑审查 → 安全检查 → [REVIEW_DONE]
```

**参数**:
- `--strict` - 攻击性审查模式

---

### /vibe-debug (问题调试)

**加载**: `ld` + `thinking/`

**流程**:
```
问题分析 → 假设生成 → 验证 → 修复 → 验证
```

---

## 🔗 与官方指令共存

VibeCoding指令与官方Claude Code指令完全兼容：

```bash
# 官方指令（保持不变）
/init
/compact
/clear
/help

# VibeCoding指令（vibe-前缀）
/vibe-plan
/vibe-code
/vibe-review
```

---

## 📝 自定义指令扩展

用户可在 `.claude/commands/` 下添加自定义指令：

```markdown
---
name: vibe-deploy
description: 部署流程
loads: ["ld", "devops"]
---

# /vibe-deploy

[指令定义...]
```
