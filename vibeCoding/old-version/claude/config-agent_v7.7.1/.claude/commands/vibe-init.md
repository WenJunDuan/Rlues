# vibe-init

## 类型
🔷 **增强官方指令** - 基于 `/init`

## 用途
初始化 VibeCoding 项目环境

## 语法
```bash
vibe-init [项目名] [--with-kb]
```

## 参数
| 参数 | 说明 |
|:---|:---|
| 项目名 | 可选，项目名称 |
| --with-kb | 同时初始化知识库模板 |

## 执行流程

```
vibe-init
    │
    ▼
┌─────────────────────────────┐
│ 1. 调用官方 /init           │ ← 确保官方功能正常
│    - 创建 CLAUDE.md         │
│    - 官方统计正常记录        │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ 2. VibeCoding 增强          │
│    - 创建 .ai_state/ 结构   │
│    - 初始化 session.lock    │
│    - 创建 kanban.md 模板    │
│    - (可选) 创建 .knowledge/│
└─────────────────────────────┘
```

## 创建的目录结构

### 基础结构
```
项目根目录/
├── CLAUDE.md                  # 官方 /init 创建
└── .ai_state/
    ├── requirements/          # 需求文档
    ├── designs/               # 设计文档
    ├── context/               # 上下文
    ├── experience/            # 经验库
    │   └── index.md
    └── meta/
        ├── session.lock       # 会话锁
        ├── kanban.md          # 看板
        └── errors.md          # 错误记录
```

### 带知识库 (--with-kb)
```
项目根目录/
├── .knowledge/
│   ├── index.md               # 知识索引
│   ├── project/               # 项目文档
│   ├── standards/             # 开发规范
│   ├── company/               # 公司要求
│   └── tech/                  # 技术栈
└── ... (基础结构)
```

## 使用示例

### 基础初始化
```bash
vibe-init my-project

# 执行:
# 1. /init → 创建 CLAUDE.md
# 2. 创建 .ai_state/ 结构
# 输出: "项目 my-project 初始化完成"
```

### 带知识库初始化
```bash
vibe-init my-project --with-kb

# 执行:
# 1. /init → 创建 CLAUDE.md
# 2. 创建 .ai_state/ 结构
# 3. 创建 .knowledge/ 模板
# 输出: "项目 my-project 初始化完成（含知识库）"
```

## 官方 /init 的增强点

| 官方 /init | VibeCoding 增强 |
|:---|:---|
| 创建 CLAUDE.md | + VibeCoding 配置追加 |
| 单文件 | + 完整目录结构 |
| 无状态管理 | + .ai_state/ 状态管理 |
| 无知识库 | + .knowledge/ 知识库 |

## 注意事项
```yaml
- 若 CLAUDE.md 已存在，官方 /init 会询问是否覆盖
- .ai_state/ 若已存在会跳过（不覆盖）
- 建议在项目开始时执行一次
```
