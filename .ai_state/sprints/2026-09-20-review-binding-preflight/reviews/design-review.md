---
schema_version: 1
mode: "design"
review_run_id: "6fa2c766-3786-4802-98b6-f640761f0432"
reviewer_target: "a71221739be2a76e6"
packet_sha256: "d7c306ecbb167e951d60a77c571a39488bc0600a461f408be59453fa2c11797e"
input_manifest_sha256: "a09f0777ce832fce51f0b3a9cb3c61148779eda8da8a036767cdd4f17e960e3c"
native_output_ref: "reviews/_native/6fa2c766-3786-4802-98b6-f640761f0432-result.json"
verdict: "CONCERNS"
---

## Native review output

Targeted design re-review — sprint `2026-09-20-review-binding-preflight`, review_run_id 6fa2c766-3786-4802-98b6-f640761f0432, mode design. Read-only, no files written. cwd confirmed `/Users/mi_manchi/workspace/Rlues`. Both input hashes match the values supplied.

Prior P0s:
- P0-1 (AC5 vs AC6 mutually exclusive): CLOSED — old AC6 is folded into AC5 (`design.md:75`) permitting exactly the two-name export addition; both gate files are now in the File Structure Plan (`design.md:81,83`); the false "imports its existing export surface" claim is replaced by an accurate account at `design.md:59`. Independently verified the bound is sufficient: CC already exports `parseFrontmatter` (`delivery-gate.cjs:1434`), Pi already exports it too (`cc-core/delivery-gate.cjs:1397`) and defines both target symbols (`:502`, `:508`), and CX needs no `.py` edit (`delivery-gate.py:1128,1141` are module-level and loaded by path at `_review_binding.py:25-28`). Two names really are enough.
- P0-2 (AC2 not implementable from persisted data): CLOSED — a persisted `input_hashes` map is now specified (`design.md:37`), the hook change is in the plan (`design.md:79`), and the old-row degradation is stated. The degradation is trivially implementable where it is needed: `assertLive(root,sprint,prepared)` already holds the row, so an undefined `input_hashes` falls back to today's message (`_review-binding.cjs:82-89`). One residual scoping gap, see new P1 below.
- P0-3 (verify-instead-of-strip breaks the gate's exactly-one rule): CLOSED by deletion — the strip is now documented as correct and untouched (`design.md:65`, Non-goals `design.md:91`, packet `review-packet.md:31`), with the prior draft's mischaracterisation explicitly retracted. Citations check out: strip at `_review-binding.cjs:176`, gate rule at `delivery-gate.cjs:525-527`, regression test at `test_state_review.py:576-590`.

Prior P1s:
- P1-1 (exclusion set completeness): CLOSED — all three paths named in WHY (`design.md:15`), HOW (`design.md:29`) and AC1 (`design.md:71`), and exclusion is explicitly applied to the stored `input_paths` (`design.md:29`), which is the part that matters because `liveInput` re-reads them at `_review-binding.cjs:75`. The `reviews/_native/*.json` carve-out is correct.
- P1-2 (AC2 over-promise): CLOSED — `design.md:38` refuses per-file claims for aggregate digests and AC2 says "makes no per-file claim" (`design.md:72`).
- P1-3 (AC3 parser under-specification): CLOSED — `design.md:44-49` specifies a root-level single-field 40-hex scan, an explicit skip on absent/malformed, the reason for not reusing `parseReviewManifest` (needs a `pathType` prepare lacks — confirmed at `delivery-gate.cjs:464,491`), and manifest byte-identity in AC3. The cited live fixture is real: `test_state_review.py:579` writes `schema_version: 1` alone.

New findings:

[P1] `design.md:57,59` + AC4 (`design.md:74`) — the governance verb's `_index.md` resolution is unspecified, and the natural implementation reads a different file than the gate does. Every existing verb resolves through `input.context`, which uses `git rev-parse --show-toplevel` (`_input-binding.cjs:34`) — inside a linked worktree that is the worktree root. The consumer of this hash resolves `fm` from `tryRepoRoot` → `--git-common-dir` (`delivery-gate.cjs:444-446`) plus `findAiState` (`:28-39`) at `:1401-1403`, i.e. the main repo. `.ai_state/` is tracked, so a System-path worktree carries its own drifting copy, and the verb would print a hash for an `_index.md` the gate never reads — the exact wrong-hash round the verb exists to prevent. AC4's "for the same `_index.md`" does not pin the resolution rule, so the test passes on a single-checkout fixture. Secondary: `input.context` also throws `current sprint unavailable` (`_input-binding.cjs:37`) when no sprint is active, though the governance hash is a pure function of `_index.md` frontmatter. Fix is one sentence in HOW: the verb resolves `_index.md` by the gate's rule (`tryRepoRoot` + `findAiState`) and parses it with the gate's already-exported `parseFrontmatter`, requiring no sprint.

[P1] `design.md:37-38` vs AC2 (`design.md:72`) — the extent of `input_hashes` is unstated, and the literal reading makes AC2's aggregate clause unachievable. AC2 requires a drifted aggregate axis to be reported "with the axis name and both hashes", but the expected values for `design_sha256` / `source_sha256` / `environment_sha256` are persisted nowhere: `liveInput` folds them into `input_manifest_sha256` and discards the map (`_review-binding.cjs:76-79`); only `packet_sha256` survives separately (`:78` → row `:120`). If `input_hashes` stores only the `fileRefs` entries — which is what "a per-path map" reads as — the code cannot even name which axis drifted. Fix is one clause: `input_hashes` persists the whole `inputs` map `liveInput` builds at `:75-77`, declared paths and the snapshot axes.

[P1] `design.md:17` vs AC2 (`design.md:72`) — a defect stated in WHY has no AC. WHY cites `evidence_ids` ("does not even name the missing id") as one of the two incidents that cost rounds, but AC2 covers only declared input paths and aggregate axes; `evidence_docs` (`_review-binding.cjs:87`) and `evidence_ids` (`:88-89`) keep their field-name-only messages. `evidence_docs` drift is the most likely real case, since `review-manifest.yaml` is one of its members (`:116`) and is edited during ship prep. Both already carry per-entry data, so naming the culprit is a filter in each case. Either extend AC2 to these two axes or drop the claim from WHY.

[P2] `design.md:75` (AC5 "asserted line by line") — testable, but it freezes a tripwire for later slices. Mechanically fine: the suite already has `REPO` (`test_state_review.py:18`) and shells out, so diffing against `git show 0ca066c:<gate>` works. The problem is lifetime: the roadmap puts slices 5 and 6 on the same `delivery-gate` and serialises them for that reason (`roadmap.md:15,76`), with slice 6 depending on slice 3. A committed "differs from `0ca066c` by exactly these two lines" test must be deleted by slice 5/6. Prefer verifying the bound once in this sprint's runtime-verify/ship evidence, or state in the design that the assertion is sprint-scoped and slice 5 removes it.

[P2] Adding the two export names is risk-free — no code enumerates the gate's exports anywhere in the tree (no `Object.keys(require(...))`, no export-list assertion in `validate-athena-9.9.9.py`). The only consumers are `_review-binding.cjs:36,112` and Pi's copy at the same lines, both by name, plus `validate-athena-9.9.8.py:407` calling `sourceDiffSha256` by name. The installer compares source/destination digests dynamically (`setup-athena.py:293`), so there is no pinned hash manifest to update. Also confirmed CC/CX governance digests genuinely agree: `JSON.stringify` over sorted-key insertion (`delivery-gate.cjs:508-512`) matches `json.dumps(..., sort_keys=True, separators=(",",":"), ensure_ascii=False)` (`delivery-gate.py:1141-1144`), and both frontmatter parsers yield strings, so AC4's cross-platform diff-equal assertion is achievable.

[P2] `design.md:71` ("Exclusion emptying the input set fails instead") — zero-input prepare is legal and is what the entire regression suite uses (`test_state_review.py:517` calls `prepare` with no `--input`). The design's "would empty" phrasing is correct, but an implementation of the form "if empty then throw" breaks every existing test. Worth pinning the condition as "declared non-empty, empty after exclusion". Related: exclusion must compare resolved paths, not raw strings, or a dot-slash prefix slips through.

[P2] Ledger and canonical-ordering checks came back clean. `input.canonical` sorts keys recursively (`_input-binding.cjs:19`), so a nested `input_hashes` map serialises deterministically and stays one line for the `events()` regex (`_review-binding.cjs:42`). Row growth is bounded and harmless: current prepared rows measure 619 bytes with two inputs; the full map adds roughly 130 bytes per declared path plus about 85 per axis, and no hook or gate caps `session-log.md` size. Critically, the addition does not change `input_manifest_sha256` (it is the digest of the local `inputs` map, not the row), so already-prepared rows stay comparable. Overlap with the existing `evidence_docs` map is possible only if an operator declares a sprint doc as an input; that duplicates a hash, which is harmless.

[P2] `design.md:13` says "Three of its failure modes" but WHY then lists five bolded problems (`:15`, `:17`, `:19`, `:21`, `:23`). Also `design.md:15` says `fileRefs` "already drops" a non-existent path; it actually throws (ENOENT at `_review-binding.cjs:61`, before the containment check at `:62`). Behaviour is still fail-closed, but the word is wrong.

Dropping the old AC4 leaves no loose end. The strip is referenced in exactly three places — `design.md:65`, Non-goals `design.md:91`, `review-packet.md:31` — and all three describe it identically and correctly. WHY no longer mentions the strip at all, so the mischaracterisation is gone rather than reworded. Pi's staleness claim is verified true by diff: Pi's `cc-core/_review-binding.cjs:175` lacks the strip and uses `const` at `:165` where CC uses `let`, so a reviewer-supplied binding line would duplicate and block ship; re-sync is safe because `cc-core/` contains every module CC's file requires, and commit `4894588` confirms the dedup provenance cited at `design.md:23`.

AC bijection: OK — AC1+AC2 → roadmap issue 6; AC3 → issue 9; AC4 → issue 10; AC5 → the standing parity invariant (`roadmap.md:15`, and Pi genuinely owns both files, so this is not fabricated symmetry). Slice 3's acceptance point now has exactly one AC group per clause, the unauthorized old AC4 is gone, and the packet's mapping table (`review-packet.md:16-20`) matches the renumbered ACs with no residual off-by-one. AC5 is compound but all three parts fall inside the same invariant and file set.

VERDICT: CONCERNS
