---
sprint_slug: "2026-09-20-heredoc-aware-shell-guard"
path: "System"
stage: "design"
author: "cc-main (rev 6: 9929ee26 两 P0 有界修复, 用户批准; 论证改正向枚举)"
base_commit: "a385d2e"
---

# Heredoc 感知 shell guard —— 窄核方案（Q12#14 + 切片 2 遗留两项）

## WHY

**误拦（原始痛点）**：pre-bash-guard 无 heredoc 语义（`findSubstitutions` CC:56-94），正文奇数反引号/未闭 `$(` → unparsable 拦截；`<<'EOF'` 正文举例的危险命令文本被当真命令拦。本会话累计 **6 次活体误拦**，全部形态为「首行单声明 + quoted 定界符」。
**现存放行洞**：`analyze` 先跑 `stripComments`，unquoted 正文行首 `#` 后的命令替换 bash 真执行、guard 却剥掉 → 放行。
**方案史（5 轮审查、同因 P0×4，档案在 reviews/）**：全文法建模路线证伪后改窄核（用户裁决）；rev 5 复核实测窄核主干成立（8 个对抗首行安全），失效点收敛为两个**有界**缺口：算术上下文逃逸（`((`/`$[`/数组下标的 `<<` 是左移非重定向）与 unquoted 豁免非单调（正文撇号激活既存引号遮蔽缺陷）。rev 6 = 两个有界修复 + 安全论证改正向枚举（用户批准）。
**切片 2 遗留②**：CX `evidence-collector.py:117` 决策前 4000 字符预截断（CC 已修模式=决策全文、落盘截 500）。
**交叉项**：切片 2 字节钉测试（`test_state_review.py:1210`）钉三端 guard 字节，本切片合法改 guard 必红，承接调整。

## HOW

### 窄形判定 `simpleHeredoc(command)`（单源 `_shell-lex`，CX `simple_heredoc`）

窄形 = 以下**全部**成立，任何一条不成立 ⇒ 不适用 ⇒ guard 与证据侧行为与今日**逐字节相同**：

1. 声明在**第一物理行**（首行必然是命令位置——之前不存在任何可延续的引号/注释/续行状态）。
2. 首行恰含一个 `<<`（或 `<<-`），非 `<<<` 的一部分；定界符紧随（`'D'`/`"D"`/裸 `D`）。**定界符 token 终点**：名=引号内字面；右引号后紧跟非空白非 shell 分隔字符（如 `<<'EOF'x`）⇒ 破坏窄形（P2-1）。
3. 首行除定界符自身引号外**无**任何引号字符、无反斜杠、无 `#`、无反引号、无 `$(`、**无 `(`、无 `[`**（rev 6：封算术上下文——见正向枚举论证；`$VAR`/`${V}` 允许，展开在运行期不改 parse 期边界，rev 5 复核实测确认）。
4. 终止行 = **物理行**与定界符名全等（`<<-` 额外允许并剥离前导 **tab**——仅 tab 不含空格；允许尾随 `\r`；**尾随空格不构成闭合**，与 bash 一致）；存在于命令内。未闭合 ⇒ 不适用（今日行为，不新增拦截）。
5. 正文 = 首行行尾换行之后至终止行前一行；首行同行内容（声明前后的命令/管道/重定向）**永不掩码**。终止行之后的所有行 = 命令上下文，照常分析。

### 正向枚举论证（rev 6，替代字符黑名单否定式）

**命题：窄形成立 ⇒ 首行的 `<<` 必处于重定向位置。** bash 文法中 `<<` 的非重定向义仅存在于**算术求值上下文**，其入口为有界集：`((…))`（含 `if/while/for ((`）、`$((…))`、`$[…]`、数组下标 `name[…]`、`let`/`declare -i` 的表达式参数。前四类分别需要字符 `(`、`$(`、`[`——均被第 3 条排除；`let`/`declare` 类经复核实测其 `<<` 仍解析为重定向（掩码正确，无需排除）。故第 3 条成立时不存在算术入口，`<<` 只能是重定向。五个算术逃逸反例（`(( 1 << 'a' ))`、`$[ 1 << 'a' ]`、`if ((…))`、`arr[1<<'i']=5`、`(( t = 1 << "$bits" ))`）全部因含 `(` 或 `[` 落「不适用」⇒ 今日行为（实测今日均拦）。

### 窄形生效语义

- **quoted 定界符**：正文整体等长空白掩码（保留换行），先于 `stripComments` 作用于原始 command；正文不进替换扫描与危险模式（bash 不展开，纯 stdin 文本）。
- **裸定界符（unquoted）——rev 6 改严格单调结构**：主扫描流**完全不变**（正文照今日处理，含注释剥离——今日能拦的全保留）；**额外**把正文以独立字符串、从干净词法态单独跑替换扫描（`findSubstitutions` 等价路径），检出的危险替换并入 danger 判定。并集结构 ⇒ 检出集只增不减（封 `# $(…)` 放行洞），且不触碰外层词法态（撇号反例：主流今日拦、继续拦）。
- 递归层（`analyze` depth>0）同规则。
- guard 消费：函数内惰性 require；载入失败/运行期异常 ⇒ 跳过（今日行为）；guard 内不得新增任何 fail-open 分支；顶层不加 require（ARCHITECTURE 惰性载入决策）。
- 证据侧：`_shell-lex.scan` 对窄形 **quoted** 正文豁免控制符分段；非窄形与 unquoted 不变。

### 其余交付

- **CX 截断修复**：`evidence-collector.py` 分类/策略吃未截断 command，仅落盘截 500；删 `:117` 4000 预截。
- **字节钉承接与记账**：删「guard 字节不变」断言，CC==Pi parity 与 gate-sans-lexer fail-closed 独立保留；措辞修订四处（P2-2 清点全）：`roadmap.md:87`、`ARCHITECTURE.md:85`、`roadmap.md:73` 验收列（「quoted heredoc 正文不误拦」→ 限窄形）、`items.yaml:60` notes（scanner 收敛已 Non-goal）；CC/CX 危险清单差异实写切片 9 notes。

## 允许写集

`vibeCoding/claude/9.9.9/.claude/hooks/{_shell-lex.cjs,pre-bash-guard.cjs}` + Pi `plugin/extensions/cc-core/` 同名两件（字节同改）；`vibeCoding/codex/9.9.9/.codex/hooks/{_shell_lex.py,pre-bash-guard.py,evidence-collector.py}`；`vibeCoding/scripts/tests/athena999/`：新建 `test_heredoc_guard.py`、`test_state_review.py` 仅改字节钉断言。`.ai_state/` 记账。
Non-goals：不建模 bash 完整命令行词法；不统一 CC/CX 危险清单差异（切片 9）；不做扫描器完整合并；不动 delivery-gate 与 CC/Pi evidence-collector.cjs；未闭合不新增拦截；既存「奇数引号遮蔽替换」底层缺陷不在本切片（主流不变原则下不激活、不修复，记 roadmap 观察）。

## 验收标准

| AC | 判据 |
|---|---|
| AC1 | 窄形 quoted 正文免疫：6 个活体误拦样本原文重放放行（先红）；正文奇数反引号/未闭 `$(`/危险命令文本不拦；**首行同行内容永不掩**（正向钉）；`<<-` 仅剥 tab 不剥空格、终止行尾随空格不闭合（正向钉） |
| AC2 | unquoted 并集结构：`# $(危险替换)` 正文今日放行、改后拦（先红）；**正文含撇号 + 危险替换今日拦、改后仍拦**（先红判据=主流不变）；正文纯文本命令字样行为与今日一致 |
| AC3 | 非窄形字节等价矩阵：五个算术逃逸反例 + 前四轮全部对抗反例（行续、双引号内反斜杠、跨行引号、注释行伪声明、内嵌声明、多 heredoc、`<<<`、转义定界、`<<'EOF'x` 残留、非首行声明、未闭合、双声明）逐一断言新旧 `analyze` 输出**完全相等** |
| AC4 | 窄形判定单源 `_shell-lex` + 函数内惰性载入 + 载入失败/运行期异常回退今日行为（缺失≠放行用例）；证据侧窄形 quoted 豁免与非窄形不变各一例 |
| AC5 | CX 决策截断修复：>4000 字符被掩盖管道回归用例 CX 记 unknown 与 CC 同判；分类/策略吃全文仅落盘截 500 |
| AC6 | 字节钉承接 + 记账四处措辞修订 + 危险清单差异实写切片 9 notes |
| AC7 | 三端一致：guard 与 `_shell-lex` 的 CC==Pi 字节相等；CX 行为同夹具判定与 CC 一致 |

## 测试场景

1. 活体样本 ×6 重放（先红）；2. unquoted 洞先红后拦 + 撇号主流不变先红；3. 非窄形等价矩阵 ≥18 形态；4. lexer 缺失/异常回退；5. 证据侧两例；6. CX 截断先红；7. 字节钉调整后全套绿。

## 风险

窄形误判两方向：误判窄=维持今日误拦（无安全损失）；误判宽由第 1/2/3 条 + 正向枚举论证封死。论证的完备性依赖「bash 中 `<<` 非重定向义仅算术上下文」这一文法事实——review 对抗验证之；若发现第五种入口即同因第五次，如实交还。
